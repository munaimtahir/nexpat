# Frontend Environment Configuration
# Copy this file to .env and update values for your environment

# Backend API Base URL
# For local development with Docker: http://localhost:8000
# For local development without Docker: http://127.0.0.1:8000
# For production: https://api.yourdomain.com or your backend URL
VITE_API_BASE_URL=http://localhost:8000

# Backend proxy for Vite dev server (used in vite.config.js)
# This is used for proxying API requests during development to avoid CORS issues
VITE_BACKEND_PROXY=http://localhost:8000

# Optional: Sentry DSN for error tracking
# VITE_SENTRY_DSN=

# Note: All environment variables used in the frontend must be prefixed with VITE_
# to be accessible in the application code via import.meta.env
